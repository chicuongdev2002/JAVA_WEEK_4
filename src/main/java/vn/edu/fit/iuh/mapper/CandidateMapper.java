package vn.edu.fit.iuh.mapper;import org.springframework.jdbc.core.RowMapper;import vn.edu.fit.iuh.enities.Adress;import vn.edu.fit.iuh.enities.Candidate;import java.sql.ResultSet;import java.sql.SQLException;import java.util.UUID;public class CandidateMapper implements RowMapper<Candidate> {    @Override    public Candidate mapRow(ResultSet rs, int rowNum) throws SQLException {        Candidate candidate = new Candidate();        candidate.setId(UUID.fromString(rs.getString("id")));        candidate.setDob(rs.getTimestamp("dob").toLocalDateTime());        candidate.setEmail(rs.getString("email"));        candidate.setFullName(rs.getString("full_name"));        candidate.setPhone(rs.getString("phone"));        // Ánh xạ trường address từ ResultSet vào đối tượng Address        Adress address = new Adress();        address.setId(UUID.fromString(rs.getString("address")));        candidate.setAdress(address.getId());        return candidate;    }}